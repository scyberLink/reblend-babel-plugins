{
  "plugins": [
    [
      "transform-reblend-jsx",
      {
        "pragma": "h",
        "runtime": "classic",
        "throwIfNamespace": true
      }
    ]
  ],
  "throws": "Namespace tags are not supported by default. Reblend's JSX doesn't support namespace tags. You can set `throwIfNamespace: false` to bypass this warning."
}
